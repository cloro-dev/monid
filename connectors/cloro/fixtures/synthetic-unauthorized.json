{
    "name": "cloro unauthorized (synthetic)",
    "description": "Synthetic cloro 401 with the documented AuthenticationError body ({ error: { code, message, timestamp } }).",
    "calls": [
        {
            "req": {
                "method": "POST",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 401,
                "body": {
                    "error": {
                        "code": "INVALID_OR_EXPIRED_API_KEY",
                        "message": "Invalid or expired API key",
                        "timestamp": "2026-09-21T12:00:00.000Z"
                    }
                }
            }
        }
    ]
}
